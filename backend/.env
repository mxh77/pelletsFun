# MONGODB_URI doit être définie comme variable d'environnement sur le serveur
# Pour le développement local, créez un fichier .env.local avec vos credentials
# Exemple : MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/database
# Ports configuration (optionnel - les valeurs par défaut sont dans config/ports.js)
PORT=3001
FRONTEND_PORT=3000