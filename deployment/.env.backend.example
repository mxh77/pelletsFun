# Configuration Backend pour Production
# À copier dans backend/.env sur le serveur

# MongoDB Configuration
# Option 1 : MongoDB local
MONGODB_URI=mongodb://localhost:27017/pelletsfun

# Option 2 : MongoDB Atlas (recommandé pour la production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pelletsfun?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=https://pelletsfun.harmonixe.fr

# Security (optionnel)
# JWT_SECRET=votre_secret_jwt_tres_long_et_complexe
# SESSION_SECRET=votre_secret_session_tres_long_et_complexe
